version: '3.8'

services:
  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: solucao42_dev
      MYSQL_USER: root
      MYSQL_PASSWORD: password
    ports:
      - "3306:3306"